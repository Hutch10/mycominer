/**
 * Marketplace extension types (re-exported from federation types for local services).
 */
export type {
  ExtensionPricing,
  ExtensionInstallation,
  MarketplaceExtension,
  ExtensionSubmission,
  SecurityScan,
  SoftwareBillOfMaterials,
  ExtensionStats,
} from '../federation/types';
